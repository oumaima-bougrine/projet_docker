# Variables d'environnement pour le projet
# Copiez ce fichier vers .env et adaptez les valeurs selon votre environnement

# Backend - Configuration du serveur
PORT=4000

# Backend - Configuration de la base de données
# Pour Docker Compose, utilisez les valeurs suivantes :
DB_HOST=db
DB_PORT=3306
DB_USER=gesture_user
DB_NAME=gesture_app
# Le mot de passe DB est géré via secrets/ dans Docker Compose
# Pour le dev local sans Docker, décommentez et définissez :
# DB_PASSWORD=votre_mot_de_passe

# Frontend - URL de l'API backend
# Utilisé lors du build Vite
VITE_API_URL=http://localhost:4000